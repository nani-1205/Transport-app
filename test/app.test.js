const assert = require('assert');

describe('Basic Transport App Tests', function() {
  it('should return 1 when 1 is returned', function() {
    assert.equal(1, 1);
  });

  // Add more tests here to test your actual app logic

});